# SPA fallback - all routes should serve index.html
/*    /index.html   200

# API proxy redirects (replace with your actual backend URL after deployment)
/api/*  https://your-backend-url.com/api/:splat  200
